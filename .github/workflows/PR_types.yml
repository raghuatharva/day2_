name: CI workflow for PRtypes
on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, closed]
    tags: ["HCLTech"]
jobs:
  show:
    runs-on: ubuntu-latest
    steps: 
      - run: 
          echo " Hello , hello 123.."
